{
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "logs": "tail -f logs/combined.log",
    "logs:error": "tail -f logs/error.log",
    "test": "jest",
    "build": "echo 'No build step required'",
    "postinstall": "mkdir -p uploads/videos uploads/thumbnails logs"
  }
}
